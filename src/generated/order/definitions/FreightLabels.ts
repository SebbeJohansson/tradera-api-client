import { FreightLabel } from "./FreightLabel";

/**
 * FreightLabels
 * @targetNSAlias `tns`
 * @targetNamespace `http://api.tradera.com`
 */
export interface FreightLabels {
    /** FreightLabel[] */
    FreightLabel?: Array<FreightLabel>;
}
